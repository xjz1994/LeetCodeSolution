/**
 * @param {number[]} bits
 * @return {boolean}
 */

var isOneBitCharacter = function(bits) {
    let str = arr.join("");
    let res = str.match(/(0|1.)/);

};

let arr = [1, 1, 1, 0];
let res = isOneBitCharacter(arr);
