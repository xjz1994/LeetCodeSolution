/**
 * @param {number[][]} grid
 * @return {number}
 */
var orangesRotting = function (grid) {
    let queue = [];
    queue.push({ x: 0, y: 0 });
    while (queue.length > 0) {

    }
};

let grid = [
    [2, 1, 1],
    [1, 1, 0],
    [0, 1, 1]
];
let res = orangesRotting(grid);
console.log(res);